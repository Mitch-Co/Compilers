/* A program to compute the factorial value of an input */

void main(void) {
  int cocks;
  int x; int fac;
  cocks = 2;
  x = 3;
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    cocks = 2;
    x = x - 1;
  } 

  output(fac);
}
