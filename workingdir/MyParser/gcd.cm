/* A prografaewfeagfesrgrsthtdrhgsrtgsdrgbsrbm to perform Euclid's 
algorithm to compute a gcd 
scannerregsgb
SYMBOLSb
bdr
bdrbd
bdrbd////
99
8
***

/***
ni

*/

int gcd(int u, int v) {
  if (v == 0) return u;
  else return gcd(v, u - u/v*v);
}

void main(void) {
  int x; int y;
  x = input();
  y = input();
  output(gcd(x, y));
}
