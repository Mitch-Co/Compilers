/* A program with multiple invalid elements */

.
triple main(void) {
  int x; int fac;
  fac = 1;
  x = [[[[[[[]]]]]]];
  properfunctioncall();

  x = wildlyimproperfunctioncall([[[]]]);

  while (x) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}